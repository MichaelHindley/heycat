[test]
coverage = true
# 100% coverage required - use coveragePathIgnorePatterns for explicit exclusions
coverageThreshold = { lines = 1.0, functions = 1.0 }
coveragePathIgnorePatterns = [
  "**/*.test.ts",
  "**/*.spec.ts",
  "**/*.test.tsx",
  "**/*.spec.tsx"
]
